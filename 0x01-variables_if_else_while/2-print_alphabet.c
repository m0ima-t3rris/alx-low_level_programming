#include <stdio.h>
/*
 * main - entry point, 
 * print alphabet in small cases.
 * return: 0 sucess.
 */
int main(void)
{
int tk;
for (tk = 'a' ; tk <= 'z' ; tk++)
{
putchar(tk);
}
putchar('\n');
return (0);
}
