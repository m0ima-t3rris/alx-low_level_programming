#include <stdio.h>

/**
 * main - Entry point.
 * 10 starting from 0, followed by new line.
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
printf("%d", i);
printf("\n");
return (0);
}
